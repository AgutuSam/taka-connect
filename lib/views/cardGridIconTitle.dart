import 'package:flutter/material.dart';

class GridIconView extends StatelessWidget {
  const GridIconView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
